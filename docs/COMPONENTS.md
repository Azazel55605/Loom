<!--
  LIVING REGISTRY — update this file in the same change as the code.

  Any time a UI component is added or modified anywhere in `apps/` or in the
  shared UI kit under `packages/ui-kit`, this table must be updated to match. This
  is not optional and is not a follow-up task: treat it exactly like updating a
  changelog. A component change with no registry change is an incomplete change.

  Column meanings:

  - Component — the exported component name, e.g. `Button`, `CommandPalette`.
  - Location  — the package or app directory that owns the implementation.
  - Source    — one of:
                  `shadcn`          used as-is from shadcn/ui
                  `shadcn-extended` a shadcn component plus new CVA variants
                  `custom`          built from scratch, following shadcn's
                                    construction pattern (Radix primitive
                                    underneath, CVA variants, shared tokens)
                See docs/UI_GUIDELINES.md for the sourcing priority order —
                `custom` is a last resort and should be rare.
  - Variants  — the CVA variants available, e.g. `default, destructive, ghost`.
                Use `—` if the component has none.
  - Consumed by — which client apps consume it: `web-frontend`, `desktop`,
                `mobile`, or a combination. This column is the point of the
                file: the shared UI kit exists to keep the three platforms
                consistent, so a component used by only one app is either
                genuinely platform-specific or an inconsistency worth noticing.
                Keep it accurate — a stale "Used in" hides exactly the drift
                this registry is meant to surface.
  - Notes     — anything a future maintainer needs: the Radix primitive it
                wraps, accessibility caveats, why it was built custom, known
                limitations.
-->

# Component registry

The living inventory of every UI component in Loom. Conventions governing how
these are built live in [`UI_GUIDELINES.md`](./UI_GUIDELINES.md) and change
rarely; this file changes constantly.

The shared React UI and client logic live in `packages/ui-kit`. Web/frontend,
Desktop, and Mobile consume that package. Each installed app keeps only thin
router, persistence, and native-transport adapters.

| Component | Location | Source | Variants | Consumed by | Notes |
| --------- | -------- | ------ | -------- | ----------- | ----- |
| `Card` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Includes `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`. Uses the `.surface-elevated` class so the blur/reduced-transparency tokens apply. |
| `Badge` | `packages/ui-kit/src` | shadcn-extended | `default`, `secondary`, `destructive`, `outline`, `healthy`, `degraded`, `down`, `unknown`, `pending` | web-frontend, desktop, mobile | `default` resolves from the accent token via Tailwind's `primary` colour. The four health variants resolve from the `--status-*` tokens in `packages/ui-kit/src/styles.css` — semantic status colours with fixed hues, deliberately **not** accent-derived, so "healthy" keeps meaning healthy when the user picks a red accent. All clients consume the extended variants from the kit.  Gained a `pending` variant for a disruptive action in flight — accent-coloured and pulsing rather than a `--status-*` token, because it is not a verdict about the service but Loom saying what it is doing. `.reduce-motion` stops the pulse globally. |
| `Skeleton` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Loading state for connector tables, dashboard tiles, dialogs, and other request-backed surfaces; shapes match the eventual controls so content does not jump when data lands. Animation is suppressed by the global `prefers-reduced-motion` rule in `packages/ui-kit/src/styles.css`. |
| `Alert` | `packages/ui-kit/src` | shadcn | `default`, `destructive` | web-frontend, desktop, mobile | Includes `AlertTitle`, `AlertDescription`. Error state for an unreachable backend; icon from `lucide-react`. |
| `Input` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Used as-is for installed-client server URLs and shared login/setup forms. Themed replacement for a native `<input>`, per docs/UI_GUIDELINES.md. |
| `Button` | `packages/ui-kit/src` | shadcn | `default`, `destructive`, `outline`, `secondary`, `ghost`, `link` (sizes `default`, `sm`, `lg`, `icon`) | web-frontend, desktop, mobile | Submit control for installed-client server selection plus shared login, sign-out, settings, and connector actions. |
| `ConnectToServer` | `packages/ui-kit/src` | custom | standard TLS, optional invalid-certificate control | desktop, mobile | Shared first-run and General-settings form. Composes `Input`, `Switch`, `Button`, `Label`, `Alert`, and `Card`; validates HTTP(S) and requires a successful `/health` response before calling the platform persistence callback. Desktop and Mobile inject native HTTP transports and enable the warning-bearing invalid-certificate option. Both persist that off-by-default choice per server and explain that it covers API traffic, while self-signed WSS live status still requires a certificate the platform trusts. |
| `BootScreen` / `BootErrorScreen` | `packages/ui-kit/src` | custom | checking / error | web-frontend, desktop, mobile | Full-screen launch feedback around `useConnectionBootstrap`. A stored/configured server is health-checked before token restoration or routing starts. Checking shows the branded spinner; failure names the URL and reason with Retry. Desktop and Mobile additionally inject Change server, while Web deliberately omits it. |
| `ConnectorStatusSocket` / `WebSocketTransport` | `packages/ui-kit/src/lib` | custom | browser, native | web-frontend, desktop, mobile | Reconnecting, reference-counted connector-status subscriptions use a required platform `WebSocketTransport` rather than constructing a browser socket in shared code. Web and Mobile inject webview-native adapters; Desktop injects `@tauri-apps/plugin-websocket` behind the same text/open/message/close/error contract. Token rotation recreates the connection and the short-lived access token remains a percent-encoded query parameter because browser WebSocket handshakes cannot set an Authorization header. Desktop's plugin and Mobile's Android WebView both support plain WS and normally validated WSS but have no runtime per-connection invalid-certificate option, so their HTTP certificate exceptions do not cover self-signed WSS. |
| `Label` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-label`, which handles the click-to-focus association a bare `<label>` only gets right by convention. Used through `FormLabel` rather than directly. |
| `Form` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | react-hook-form bindings: `Form`, `FormField`, `FormItem`, `FormLabel`, `FormControl`, `FormDescription`, `FormMessage`, `useFormField`. Taken as-is for the accessibility wiring — it associates an error message with the input that produced it via `aria-describedby`/`aria-invalid`, which is exactly what rots when written per form by hand. Validation is zod through `@hookform/resolvers`. |
| `Toaster` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Sonner, mounted once in `main.tsx`. Reads `useAppearance()` for the resolved light/dark palette and passes it to Sonner's `theme` prop — the same thing shadcn's own wrapper does with `next-themes`. Our classes set the toast background, but Sonner ships defaults keyed off that prop at the same specificity, so leaving it on `light` risks a white toast with light text the moment the two rules are ordered differently. Sonner is shadcn/ui's current toast component upstream, having replaced the older `Toast`/`useToast` pair. Restyled from the shared tokens (`.surface-elevated`, `--border`, accent) rather than Sonner's own palette, so the blur and reduced-transparency settings reach it. Respects `prefers-reduced-motion`. |
| `AppShell` | `packages/ui-kit/src` | custom | desktop expanded/collapsed; mobile drawer open/closed; compact phone landscape | web-frontend, desktop, mobile | The signed-in chrome: full-width sticky header, backend core version, current user, and sign-out. Home, settings, and optional sidebar content are injected as React nodes, so the shared package has no dependency on React Router or a browser navigation model. All three clients supply `DashboardSidebar` and their current route as an opaque navigation key. At `md` and above, the sidebar is anchored to the viewport's left edge and can be collapsed with the header trigger or Ctrl/Cmd+B; that per-device choice is persisted best-effort in `localStorage`. Below `md`, it becomes a focus-trapped `Sheet` drawer that starts closed, opens from a menu button, and closes after route navigation. Mobile's platform stylesheet applies Android status/navigation-bar and display-cutout safe areas to the shell, drawer, full-screen states, and portalled dialogs. Short landscape phone viewports hide the desktop-height header/sidebar and expose a 44px floating navigation trigger; tablets retain the standard shell. Uses `.surface-elevated`/`.surface-panel` so the blur tokens reach both header and sidebar. |
| `DashboardSidebar` | `packages/ui-kit/src` | custom | shell expanded/collapsed; section expanded/collapsed | web-frontend, desktop, mobile | Collapsible Pinned, My Dashboards, and Shared with me sections backed by `GET /dashboards`. The whole panel is collapsed by `AppShell`, independently of its section toggles. Pinned entries deliberately also remain in their ownership section. Pin changes are optimistic React Query mutations and role badges distinguish Editor/Viewer shares. Navigation and the optional platform footer are injected, keeping React Router out of the shared package; Mobile uses that footer for connector management, settings, and sign-out so every action remains reachable when compact landscape mode hides the header. Owns the small new-dashboard dialog and navigates through its callback after creation. |
| `DashboardCreateDialog` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Reusable dashboard-name `Dialog` used by the sidebar and the `/dashboards` first-dashboard empty state. Creates through the shared API client, refreshes the dashboard query, and returns the created summary to platform navigation. |
| `DashboardsIndexView` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Shared `/dashboards` loading, failure, first-dashboard, and preferred-dashboard selection states. Navigation is returned through a callback so browser history versus Tauri hash routing remains app-owned. |
| `DashboardView` | `packages/ui-kit/src` | custom | read-only grid / edit-layout / grouping-selection modes | web-frontend, desktop, mobile | Dashboard detail: header with owner-only inline rename, share, and confirmed delete, plus a responsive `react-grid-layout` containing both standalone `PlacementTile`s and composite `GroupTile`s. Distinct `placement-`/`group-` keys route settled geometry to the correct PATCH endpoint. The grid is static until an Owner or Editor enters Edit layout; Viewers never see editing or grouping controls. A second Group tiles mode appears with at least two standalone placements, disables drag/resize, gives every standalone tile a controlled Radix `Checkbox`, preserves click order as member order, and creates the group from the selected tiles' clamped bounding box. Its sticky count action clears on cancel, dashboard navigation, or success. It uses 6/4/2/1 columns as the container contracts; phone breakpoints stack every top-level tile full-width, while the six-column geometry remains canonical. Group minimum width is the sum of member connector minima capped at six columns; extra members wrap inside instead of widening the dashboard. A cold fetch explicitly remeasures after the grid enters the DOM. Edit mode disables action widgets and exposes the 44px resize target. One status-socket subscription covers standalone and grouped instances and passes readings to every member. On drag/resize stop the whole compacted layout is diffed and placements/groups are persisted through their respective endpoints; failures toast and refetch. Owns create-group, add-to-group, `AddPlacementDialog`, `PlacementBindingsDialog`, and remove-placement flows. |
| `DashboardSharesDialog` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Owner-only share list and add/remove flow. Uses `SegmentedControl` for User/Group, existing `getUsers`/`getGroups` data in shadcn `Select`s, View/Edit roles, and `AlertDialog` before revocation. Backend errors remain visible at form level, including the 409 message for a duplicate target. |
| `ConnectorsView` | `packages/ui-kit/src` | custom | grouped / filtered / selecting / bulk progress | web-frontend, desktop, mobile | Standalone connector administration at `/connectors`, now a shadcn `Table` rather than a card grid. Rows are grouped under DashboardSidebar-pattern collapsible headers by connector type, with the type catalog's icon/display name and a count; groups stay alphabetical while Name A→Z, Name Z→A, or problem-first Status sorting applies within each group. Live search matches instance names or tags case-insensitively; active tag chips use OR with each other and AND with search, and empty groups disappear. Rows show the resolved instance icon/name, up to two tag `Badge`s plus `+N more`, live status, last checked, and manage-gated discover/edit/delete icons. Degraded status details use the shared clickable `ConnectorStatusBadge` rather than occupying a permanent second line. Select mode adds Radix `Checkbox` cells and a select-all checkbox scoped strictly to currently visible filtered rows. A sticky toolbar exposes count, clear, and manage-gated bulk delete. Bulk deletion uses one `AlertDialog`, names every affected instance, runs requests sequentially, attaches pending/success/backend-failure feedback per row, refreshes list/tag caches after each success, and keeps partial failures open for retry. The shared WebSocket still updates list/detail caches without polling. Empty/error/skeleton states remain explicit, and `renderShell` keeps platform routing outside the package. |
| `ConnectorCard` | `packages/ui-kit/src` | custom | — | — | Retained as a source-compatible shared component, but no current route renders it after `ConnectorsView` moved to the grouped table. It remains available for a future compact connector summary surface and still supports host-level actions; dashboard interaction uses `PlacementTile` instead. Status uses the same shared badge-and-info control as current routes. |
| `ConnectorStatusBadge` | `packages/ui-kit/src` | custom | plain / explained | web-frontend, desktop, mobile | Composes the health `Badge` with a labelled info-icon `Button` only when `connectorAvailability` supplies a Degraded reason. Clicking or tapping opens a shadcn `Popover` containing the status label and explanation; healthy, pending, Down, and unexplained states remain a plain pill. Shared by connector management, dashboard tiles, the expanded detail dialog, and the retained `ConnectorCard`, keeping the explanation compact without depending on hover. |
| `SchemaForm` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | A form generated from a JSON Schema document, so registering a connector type on the backend needs no frontend change: the add-connector dialog renders whatever `configSchema` it is handed, and action parameters go through the same component. Composition of `Input` + `Switch` + `Label`; recursive over nested `object` properties. An optional discovery descriptor replaces one matching string input with `DiscoveryFieldPicker` while preserving the ordinary controlled input and live form values. Every property's optional JSON Schema `description` renders directly beneath its control in muted helper text, remains visible beside validation errors, and is connected to the control with `aria-describedby`; fields without one retain their prior layout. Also exports `defaultsForSchema` and `validateSchemaValues`. **Minimal JSON Schema support: string, number/integer, boolean, and nested objects only.** `enum`, `array`, `oneOf`/`anyOf`/`allOf`, `$ref` and unrecognised types are rendered as a visible "not supported yet" note rather than silently dropped — a field quietly missing from a form is a setting a user can neither set nor see that they cannot set. Validation covers `required` and basic types only and is **not** a JSON Schema validator: the connector's own factory on the backend is authoritative, and its 400 is surfaced by the caller. |
| `DiscoveryFieldPicker` | `packages/ui-kit/src` | custom | loading / error / empty / results | web-frontend, desktop, mobile | Generic assistance for a discoverable schema string field: keeps the normal `Input`, adds a Browse `Popover`, fetches only when opened, delegates search and result selection to `SearchablePickerList`, and writes the chosen resource's opaque `targetFieldValue` through the controlled form callback. The trigger stays disabled until the caller's candidate configuration can support discovery. Shape-matched skeleton rows, a backend-message `Alert`, and an explicit empty result keep every request state visible. It knows neither Docker nor any connector-specific configuration key. |
| `SearchablePickerList` | `packages/ui-kit/src` | custom | empty / results / selected | web-frontend, desktop, mobile | Internal controlled search-and-select list shared by `DiscoveryFieldPicker` and the add-placement sub-target picker. Filters display labels case-insensitively through a deferred search value, exposes selection with themed `Button` rows and `aria-pressed`, and leaves request loading/error ownership to its caller. An option may carry an optional `badge` — the add-placement picker sets it to the sub-target's title-cased `kind`, so a mixed Container/Stack list reads as one; `ml-auto shrink-0` beside a wrapping label keeps the badge from crowding the name at narrow widths. Optional because the discovery picker's options are all the same sort of thing and would gain only noise. **Search still matches `label` alone**: the badge describes a row, it is not another thing to type at. |
| `SetupGuidePanel` | `packages/ui-kit/src` | custom | variant tabs | web-frontend, desktop, mobile | Container for a type's complete `SetupGuide`: renders one responsive shadcn `Tabs` trigger per independent setup variant and keeps each variant's local toggle state mounted while switching tabs. Receives the connector type id and the same live schema-backed values as `SchemaForm`, then delegates each path to `SetupGuideVariantPanel`. Empty variant arrays render nothing. |
| `SetupGuideVariantPanel` | `packages/ui-kit/src` | custom | static / declarative / testing / success / error | web-frontend, desktop, mobile | Renders one setup path's description, UI-only shadcn `Switch` rows with optional Recommended `Badge`s, instant AND-only capability summary, live plain-text template, Copy action, and an independent Test connection action. Toggle defaults stay local and never enter connector configuration. Templates combine live schema values with each toggle's `envVar` mapped to `1`/`0`; empty config values retain the visible `<fieldName>` convention. The live check sends only schema-backed values to the ephemeral type endpoint, exposes pending state in the button, shows the backend's real failure in an `Alert`, and resets stale results when the form changes. |
| `CapabilityStatusList` | `packages/ui-kit/src` | custom | available / unavailable | web-frontend, desktop, mobile | Shared semantic list for both declarative toggle predictions and live `test_connection()` results. Available capabilities use a check icon and label; unavailable rows use a muted X, label, and optional note, so the source of the capability result never changes its visual language. |
| `DiscoverResourcesDialog` | `packages/ui-kit/src` | custom | loading / empty / selecting / sequential progress / completed | web-frontend, desktop, mobile | Runs instance-scoped discovery on open and reads proposals from the shared `{ discoveryTargetField, resources }` response used by both discovery endpoints. Shows shape-matched `Skeleton` rows while pending, and represents each suggestion with a Radix `Checkbox`, suggested name, target type, and a compact top-level key/value config summary rather than raw JSON. Select-all/none operate on resources not already created. Add selected issues ordinary `createConnectorInstance` requests sequentially, attaching pending/success/backend-error feedback to the responsible row; successful rows cannot be repeated, failed rows remain selected for retry, and completion reports `N of M`. The connector list is invalidated in the background after every attempted batch, and the dialog stays open for inspection and manual closing. |
| `ConnectorInstanceDialog` | `packages/ui-kit/src` | custom | generic add / edit | web-frontend, desktop, mobile | One dialog for both adding and reconfiguring an instance, as with `GroupDialog` — the fields and validation are identical and only the request differs. Type list comes from `GET /connector-types` through a `Select` and the configuration form from `SchemaForm`; integrations remain catalog/schema-driven. A connector instance configures one remote server; sub-target selection belongs to `AddPlacementDialog`. The type is fixed when editing and auto-selected when only one is registered. When a type publishes a guide, responsive full-width `Configuration` and `Setup guide` tabs replace the former side-by-side columns in both add and edit flows; both panels remain mounted so live schema-backed values survive tab switches and continue feeding template substitution. Types without a guide show only the configuration form and no empty tab. The dialog expands to a maximum 896px while retaining a viewport-relative width and scrollable height on phones. Guide toggles remain UI-only and are never submitted. Collects `name` separately from connector config and surfaces connector validation as a form-level `Alert`. **When editing**, also hosts `ConnectorIconPicker` and `TagChipEditor`; tag suggestions come from the shared `connector-tags` query, while suggestion failure leaves free-form entry working. The update always submits the complete tag set and all edit state is re-seeded when the dialog opens for another instance. |
| `TagChipEditor` | `packages/ui-kit/src` | custom | empty / assigned / suggestions | web-frontend, desktop, mobile | Controlled free-form tag editor used by `ConnectorInstanceDialog`: assigned values render as removable secondary `Badge` chips, an `Input` adds on Enter or comma (and Backspace removes the last chip when empty), and matching in-use tags appear as themed suggestion `Button`s while typing. Suggestions are optional assistance, not a closed vocabulary; brand-new tags remain valid. Exact duplicates are suppressed before the replace-all update is sent. |
| `PlacementTile` | `packages/ui-kit/src` | custom | read-only / editing / grouping selection / grouped member | web-frontend, desktop, mobile | Reusable renderer for one placed connector, used both as a standalone grid item and inside `GroupTile`: card shell (`ConnectorIcon` at 20px, name, type, health `Badge`) plus every bound widget through `renderWidget`. A sub-target placement appends its target to the type subtitle, making multiple containers from one connector distinguishable — rendered through `describeTarget`, so a Docker stack reads `shop (stack)` under a `Layers` glyph instead of the connector icon, and anything else renders exactly the bare id it always did. Derived from the id client-side with no extra fetch: a header that had to load the sub-target list to title itself would put a request, a loading state and a failure state on a dashboard that already has what it needs. Draws on the placement, its own consistently keyed `["connector-instance", id]` detail query, and live status passed from `DashboardView`; this keeps every group member independently live and cache-sharing with `ConnectorCard`. It selects status readings from the placement target's nested detail bucket and filters data-point/action descriptors to that exact target before rendering. Action execution includes the placement `targetId`. Loading is a title-and-widget-grid skeleton and retained values prevent sparse frames regressing to loading. Standalone edit mode supplies the grid drag handle, binding/remove actions, and an Add to group action when groups exist. Grouping-selection mode replaces those actions with a controlled Radix `Checkbox`. Group members never become nested drag handles; in edit mode they expose left/right reorder, binding edit, and remove-from-group controls. Action widgets are disabled throughout editing. `DashboardPlacementCard` remains only as a deprecated source-compatible re-export. Health badge and action availability come from `connectorAvailability()`: a `pendingOperation` shows as "Performing: <label>" and outranks health, while a Down connector disables its action widgets with an explanatory tooltip. Degraded reasons open through `ConnectorStatusBadge`; Down network diagnoses remain visible muted text. A **host-level** tile (`targetId === null`) whose connector declares an `updates` resource kind also renders `UpdatesSummary` above its widgets — a waiting-updates badge and an inline whole-kind action — and renders nothing extra when the kind is absent or currently empty. |
| `GroupTile` | `packages/ui-kit/src` | custom | read-only / editing | web-frontend, desktop, mobile | Composite top-level grid card for one `DashboardPlacementGroup`. Its header shows the persisted group name and resolved icon, is the sole drag handle, and offers a shadcn edit dialog plus confirmed Split apart in edit mode. Its member summary includes each non-null target alongside the connector name — through the same `describeTarget` its children use, so one placement does not read two ways depending on whether somebody grouped it — and its ordinary `PlacementTile` children repeat that target in their own subtitle. The dialog renames the group and assigns or clears a curated generic icon through `GenericIconPicker`; both persist through the existing group PATCH. The body is a wrapping flex row of ordinary `PlacementTile`s with a 16rem basis, so members share width when space permits and wrap without introducing a nested grid. Reorder sends the complete swapped `memberOrder`; remove-member and split always refetch because removing from a pair can auto-dissolve the group and return both placements to standalone. The group width floor is documented and calculated by `DashboardView`. |
| `ConnectorDetailModal` | `packages/ui-kit/src` | custom | responsive expanded view | web-frontend, desktop, mobile | Large internally scrolling shadcn `Dialog`, near-full-screen on phones. Shows the instance's `ConnectorIcon` at 22px beside its identity (including a non-null placement target), health, last-check time and display fields; selects the target's nested status details, filters descriptors/actions to that target, renders every placement binding through `renderWidget` in expanded mode, and adds unbound actions as plain button widgets. Target-aware action execution uses the placement's `targetId`. Holds a reference-counted status-socket subscription only while open and releases it on close. Badge shows "Performing: <label>" while a disruptive action runs. Degraded explanations open from `ConnectorStatusBadge`; the Down `diagnosis` remains muted text beneath the header. An optional `focus="logs"` prop scrolls the body to this placement's first `logStream` binding on open — set by `LogPreviewWidget`'s expand affordance, so opening a log lands on that log rather than on whatever binding happens to be first.. Below the widgets, one `Tabs` section per declared resource kind, each holding a `ResourceKindBrowser` scoped to the placement's `targetId`; a connector that browses nothing renders no tab strip at all. Kinds are filtered by their declared `applicableTarget` against the modal's own context — `hostOnly` kinds only when `targetId` is null, `targetOnly` only when it is set, `any` always — so a container's modal does not offer the daemon's Images tab, and an unrecognised value is shown rather than hidden. Passes `onOpenTarget` to each browser and renders a **nested** `ConnectorDetailModal` for the chosen sub-target, focused on its log — its placement is synthesised from the host's with `targetId` swapped and a single `logStream` binding, because the rows of a host's log table are mostly containers on nobody's dashboard and waiting for one to be placed would make the table a list of things you cannot open. The container's own controls still arrive under "Other actions", which come from the connector's descriptors rather than the bindings. Cannot recurse: the only kind with clickable rows is `hostOnly`. Its resource-kind query is keyed by **target as well as instance** and sends `?targetId=`, because what a connector publishes can depend on which view is open — a Docker stack has a Members table its own containers do not. Its title renders the target through `describeTarget` and swaps in the `Layers` glyph for a stack, matching the tile it was opened from. |
| `ResourceKindBrowser` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Generic table view for any connector's declared resource kinds, driven entirely by backend descriptors — no per-connector-type frontend code. Columns and their formatting come from `ColumnDescriptor.valueType`: `bytes` scales to KiB/MiB/GiB with the exact count in the title, `timestamp` renders localized with the ISO instant in the title, `bool` becomes a check/X with screen-reader text, `number` is locale-formatted and `tabular-nums`. Row actions submit `resourceId` **automatically** from the row they were clicked on — never typed by the user — along with any parameter whose name matches a **column key on that row**, and open `ActionParamsDialog` only for what is genuinely left to ask. An action every one of whose parameters the row answers runs on click, which is what makes "apply this update" and "roll this back" single clicks rather than forms asking for a value printed in the next cell. A row's `targetId` field names the sub-target its actions address; the row **id** is never guessed at for that, because a table's rows are not always targets — `recentlyUpdated` is keyed by log entry. Kind actions render as buttons above the table. When the descriptor sets `groupByKey`, rows are gathered under collapsible group headings — the same in-table header row, ghost button, rotating `ChevronDown` and count `Badge` the connectors screen uses, so a grouped table reads as the same idea rather than a second invention; an ungrouped kind renders the identical flat table it did before. Groups start **collapsed** — a kind is grouped because its list is long, and opening all of it by default hands the reader the wall of text the grouping exists to spare them; expanded state is what is remembered, so a group appearing between two refreshes starts closed like the rest. A group heading also renders the descriptor's `groupSummary` values, read off the group's first row rather than aggregated here, because rows do not carry enough to aggregate correctly. A `status` cell renders as a `Badge`, its tone mapped onto the existing `healthy`/`degraded`/`down`/`unknown` variants rather than four visually identical new ones — those resolve to the `--status-*` tokens, which are the palette's good/warning/bad/neutral roles. Every kind gets a **search box** filtering on the text a reader can actually see (each declared column's rendered value, the grouping value, and a status pill's label — never the raw `fields` object or a row id nobody typed), all terms required in any order; while a search is active every group is open, since a match hidden inside a closed group is indistinguishable from no match. **One deliberate kind-specific exception**, named as a constant so it can be grepped: for `descriptor.kind === "logs"`, the `latestLogLine` cell renders in `LogPreview`'s own monospace-and-truncate treatment (a log line in the body font, wrapped across three rows, is a different and worse thing), the row itself becomes a `role="button"` opening that container's detail view via the optional `onOpenTarget` prop — the same destination `LogPreview`'s expand icon reaches from a dashboard tile — and a `Maximize2` glyph mirrors that affordance. This was not promoted into the descriptor (`ColumnValueType.LogLine`, a `rowOpensTarget` flag) because one case is not a pattern; a second connector wanting either is the moment to generalise, with two real uses to design against. An empty result is a sentence, not an empty table; a failure is the backend's own message in an `Alert`. |
| `UpdatesSummary` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | The waiting-updates badge and inline whole-kind action on a host `PlacementTile`. Counts rows in the connector's `updates` resource kind on a 60s poll, deliberately decoupled from the WebSocket status stream: update availability is established by a scheduler that talks to a registry every few hours, so it has nothing to gain from status cadence. Renders `null` at zero rows, so a connector with nothing waiting looks unchanged. The button invokes the kind's *first* declared kind action, whatever it is called — nothing here knows Docker. |
| `ConnectorIcon` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Resolves `brand:`/`lucide:` icon references — see [`THIRD_PARTY_ICONS.md`](./THIRD_PARTY_ICONS.md). Resolution order is `iconOverride`, then the type's `metadata.icon`, then a hard `lucide:server` fallback, and a reference naming something this build lacks (an unvendored `brand:` key, a `lucide:` name outside `GENERIC_ICONS`, an unprefixed legacy string) falls through to the *next candidate* rather than straight to the default — so an override for a missing brand still shows the type's own icon. Never throws and never renders nothing: a connector is not broken because its icon is. **Brand icons are not tinted.** A `lucide:` icon is a line drawing inheriting `currentColor` and follows the accent like every other icon; a `brand:` icon is someone else's mark with its own palette, so it renders at natural colours on a neutral plate — the accent colours our surfaces, not their logo. Brand SVGs are `import()`ed from a hand-written `BRAND_ICONS` map (greppable, one lazily-loaded chunk each — a deployment vendoring twenty ships none of the nineteen it is not drawing) and cached at module scope, so only the first render of a brand waits; the space is held by an empty box of the final size rather than the fallback icon, which would flash a server and swap to a whale. Markup is inlined with `dangerouslySetInnerHTML` so a logo can be sized by its container, guarded by a shape check — the input is a reviewed in-repo file, never an API, connector, or user value. A failed chunk load leaves the placeholder rather than throwing, since there is no error boundary here. |
| `GenericIconPicker` / `ConnectorIconPicker` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Reusable grid of the seventeen `GENERIC_ICONS` plus a leading caller-labelled default tile. Radix `RadioGroup` reskinned as tiles gives exactly one selection, arrow-key movement and one tab stop. `null` is a first-class default choice represented by a non-colliding sentinel, so both connector overrides and group icons can be cleared without a separate reset control. The connector wrapper supplies connector-specific labels and the type icon; `GroupTile` supplies its boxes fallback. Unknown stored references remain visible in a selected one-off tile instead of being silently overwritten. Selection uses an accent ring rather than a fill. **Offers generic icons only:** brand icons identify a product and are not user-assigned labels. |
| `connectorAvailability` | `packages/ui-kit/src/lib` | custom | — | web-frontend, desktop, mobile | Not a component — the one function that turns a status reading into what to show and whether it should let anyone press anything. Exists because "a pending operation outranks health" is exactly the kind of rule that gets implemented three times and fixed once: `ConnectorCard`, `ConnectorsView`, `PlacementTile` and `ConnectorDetailModal` all read it. Returns the badge label and tone (including the non-health `pending` tone), whether actions are disabled, the tooltip sentence explaining why, the network `diagnosis`, and a visible `statusReason` for Degraded readings. The reason comes from the host status detail's diagnostic `error`, with a generic reachable-but-partial fallback for connectors that publish Degraded without one. Down disables actions; pending and Degraded do not. |
| `renderWidget` | `packages/ui-kit/src/widgets` | custom | compact / expanded | web-frontend, desktop, mobile | The dispatcher every widget reaches the page through and the single place the binding model meets the primitives. Display bindings with no first value and action bindings whose action list is unresolved render the matching skeleton variant. Expanded mode gives charts, gauges and stat values more room in `ConnectorDetailModal`, and is what selects between the two log renderings: a `logStream` binding draws `LogPreviewWidget` on a compact grid placement and the full `LogStreamWidget` when expanded — one binding, drawn for its context, not a widget type the user picks. The optional `onExpand` callback is what gives the preview its affordance; a context with nowhere to expand into omits it and the button does not appear. Unknown resolved bindings remain labelled rather than disappearing.  Action widgets are disabled **with a themed tooltip** when `unavailableReason` is set — setting it also disables them, because a reason not to allow something and the disabling are one fact, and letting them diverge is how a control ends up greyed out with no explanation. The tooltip trigger is a wrapper `span` around the control rather than the control itself: a disabled element fires no pointer events, and `tabIndex={0}` keeps the explanation reachable by keyboard. A themed tooltip, not `title` — a native tooltip is a browser-default control per UI_GUIDELINES and renders differently on each of the three platforms. |
| `getCompatibleWidgetTypes` / `getCompatibleActionWidgetTypes` | `packages/ui-kit/src/widgets` | custom | — | web-frontend, desktop, mobile | The two lookup tables `PlacementBindingEditor` offers from, in `widgets/compatibility.ts` alongside the display/action widget labels and the `DisplayWidgetKey` ↔ `DisplayWidgetType` conversions (the chart variant is an object, not a string). Number → stat tile, progress bar, gauge; String → stat tile, log stream; Bool → status dot, stat tile; TimeSeries → chart. For actions it is a **heuristic over the same minimal schema slice `SchemaForm` supports** — one top-level `string`/`number`/`boolean` property — and everything it cannot read falls back to `button`, the one control that handles any action by opening the generated form. `button` is therefore always present and always last. Guidance, not enforcement: the backend validates that ids *exist*, never that a widget suits its data. |
| `StatTileWidget` + `StatTileSkeleton` | `packages/ui-kit/src/widgets` | custom | sizes `sm`, `md`, `lg` | web-frontend, desktop, mobile | One reading shown large with its label, and the fallback for anything scalar. Its shape-matched skeleton preserves the value-and-label footprint before the first reading. `tabular-nums` prevents live numeric jitter. A descriptor unit of `bytes` is presentation metadata: the exact API number remains unchanged while the tile scales it to B/KiB/MiB/GiB/TiB/PiB with compact locale-aware precision. This covers Docker container memory and host disk usage without connector-id special cases. |
| `ProgressBarWidget` + `ProgressBarSkeleton` | `packages/ui-kit/src/widgets` | custom | — | web-frontend, desktop, mobile | A bounded number as a filled `Progress` bar over `config.min`–`config.max`, with the raw reading and unit beside it. The skeleton preserves label, number and track geometry. |
| `MetricChartWidget` + `MetricChartSkeleton` | `packages/ui-kit/src/widgets` | custom | `line`, `bar`, `pie`; compact / expanded | web-frontend, desktop, mobile | Lazy Recharts renderer. The skeleton extends `ChartPlaceholder` and keeps the chart canvas footprint; expanded mode gives the popout a taller canvas. Colours come from tokens and update animation stays disabled. |
| `GaugeWidget` + `GaugeSkeleton` | `packages/ui-kit/src/widgets` | custom | sizes `sm`, `md`, `lg` | web-frontend, desktop, mobile | A bounded number as an accessible 270° radial dial; the circular skeleton preserves its footprint. CVA carries sizes and both arcs use token colours. |
| `StatusDotWidget` + `StatusDotSkeleton` | `packages/ui-kit/src/widgets` | custom | — | web-frontend, desktop, mobile | Token-coloured state dot plus text so status is never conveyed by colour alone. The skeleton mirrors dot, value and label. |
| `LogStreamWidget` + `LogStreamSkeleton` | `packages/ui-kit/src/widgets` | custom | compact / expanded | web-frontend, desktop, mobile | Scrolling monospace pane over a newline-separated string. **Height is capped** (`max-h-[14rem]` compact, `max-h-[28rem]` expanded) with `overflow-y-auto`, so a long log scrolls inside the pane instead of stretching its tile and the grid rows around it. Auto-scroll follows only when already at the bottom. Skeleton and expanded variants preserve/show a larger log surface. On a compact grid placement `renderWidget` draws `LogPreviewWidget` instead; this full pane is what the expanded (modal) path renders. |
| `LogPreviewWidget` + `LogPreviewSkeleton` | `packages/ui-kit/src/widgets` | custom | — | web-frontend, desktop, mobile | Compact single-line variant of LogStream, used for grid placements; full scrollable view remains in ConnectorDetailModal. Shows the most recent non-empty line, truncated, in LogStream's monospace at a StatTile's footprint, with an expand affordance that opens the detail modal focused on the log (`renderWidget`'s `onExpand`, `ConnectorDetailModal`'s `focus="logs"`). Not a widget type a user selects — the same `logStream` binding, rendered for the context it is in. |
| `ActionButtonWidget` + `ActionButtonSkeleton` | `packages/ui-kit/src/widgets` | custom | — | web-frontend, desktop, mobile | Runs any action, opening `ActionParamsDialog` when parameters are required. Its skeleton is also reused by `ConnectorCard` while actions resolve. |
| `ActionToggleWidget` + `ActionToggleSkeleton` | `packages/ui-kit/src/widgets` | custom | — | web-frontend, desktop, mobile | Optimistic boolean `Switch` that reverts on failure. Its skeleton mirrors label and switch before actions resolve. |
| `ActionSliderWidget` + `ActionSliderSkeleton` | `packages/ui-kit/src/widgets` | custom | — | web-frontend, desktop, mobile | Numeric `Slider` that fires on release, never during drag. Its skeleton preserves label, value and track. |
| `ActionTextFieldWidget` + `ActionTextFieldSkeleton` | `packages/ui-kit/src/widgets` | custom | — | web-frontend, desktop, mobile | Input-and-submit action form; clears on success and keeps rejected text. Its skeleton mirrors the label, field and icon button. |
| `ActionSelectorWidget` + `ActionSelectorSkeleton` | `packages/ui-kit/src/widgets` | custom | — | web-frontend, desktop, mobile | Fixed-option shadcn `Select`; a binding with no options is explicitly disabled. Its skeleton mirrors label and trigger. |
| `PlacementBindingEditor` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Builds the `widgetBindings` array for a placement, shared by `AddPlacementDialog` (host bindings seeded from the connector's `defaultLayout`) and `PlacementBindingsDialog` (seeded from what is stored) — one editor, because the flows differ only in where the starting array came from. Its required placement `targetId` filters both data points and actions before any picker or Add row sees them: host and one sub-target are mutually isolated. A row per binding: a Display/Action `SegmentedControl`, a `Select` for the data point or action, a `Select` for the widget filtered by the compatibility tables, and the config fields that widget needs (gauge/progress bar min–max, slider min–max–step, chart type, a repeatable option list for a dropdown). Changing the selected descriptor re-picks the widget when the current one no longer fits, rather than leaving a pairing that would render nothing. Rows carry a **minted stable key** rather than their array index: Radix `Select` keeps state by component identity, and a reused instance whose controlled value is no longer among its items clears itself and reports `""` — which is how removing one row could submit an empty widget type for another. Every `onValueChange` also rejects `""` for the same reason. |
| `AddPlacementDialog` | `packages/ui-kit/src` | custom | server info / one sub-target | web-frontend, desktop, mobile | Adds a connector to a dashboard. A connector without sub-target support behaves as before: choosing it reveals host widgets pre-filled from `defaultLayout`, and both submit together. A connector with `supportsSubTargets` adds a `SegmentedControl`, defaulting to Server info; **Container / Stack** fetches `GET /connector-instances/{id}/sub-targets`, showing shape-matched skeletons or a backend-message `Alert`, and uses `SearchablePickerList` for the result. Each row carries a `Badge` of the sub-target's title-cased `kind` — the connector's own word, so an unfamiliar one still renders rather than being dropped — which is what lets one list hold containers and stacks without them reading as interchangeable. The mode label and surrounding copy say "container or stack" rather than "container" for the same reason; search continues to filter on `label` alone. Target creation sends `targetId` and omits `widgetBindings`, leaving the backend's `default_layout_for(targetId)` as the single source of target defaults. Auto-selects when only one instance is registered. Size defaults to `metadata.minSize`, the same floor the grid and the backend enforce, so a new card is never born too small to read; position is the crude "below the lowest existing top-level tile", including placement-group boxes, so a dashboard containing only groups does not add its next connector back at row zero. `noValidate`, so the backend's own message about a refused binding or size is what the user reads. |
| `PlacementBindingsDialog` | `packages/ui-kit/src` | custom | host / sub-target read-only | web-frontend, desktop, mobile | Edits the widgets on an existing placement, sharing `PlacementBindingEditor`. Shows the current view as a read-only Server info/target-id `Badge`, and only the exact target's descriptors are offered. PATCHes **only** `widgetBindings` — position and size belong to the grid, and sending them from here would undo a drag that happened while the dialog was open. Which connector or sub-target a placement points at is deliberately not editable: changing it requires re-seeding every binding, so delete-and-add is the current path and is documented beside the control in code. |
| `ActionParamsDialog` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Collects an action's parameters before running it, generated from its own `paramsSchema` through `SchemaForm` so an action gaining a parameter needs no frontend change (same string/number/boolean subset). Extracted from `ConnectorCard` so `ActionButtonWidget` shares it. `action === null` renders nothing, so a caller can keep it mounted and let the selected action drive it. `noValidate`: native bubbles would pre-empt the connector's own `invalidParams` wording. Also exports `takesParameters`. |
| `Progress` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-progress`. Radix owns `role="progressbar"` and the value attributes, which is the whole reason this is not a styled `div` — a screen reader gets the number without the caller doing anything. Fill resolves from `--accent` via Tailwind's `primary`. Consumed by `ProgressBarWidget`. |
| `Slider` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-slider`; the themed replacement for `<input type="range">`. Radix carries the keyboard behaviour a range input would otherwise give for free (arrows, Home/End, Page Up/Down) plus the ARIA value attributes, so a themed track loses nothing. Separates `onValueChange` from `onValueCommit`, which is what lets `ActionSliderWidget` avoid firing a request per frame. |
| `SetupForm` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Exported first-run form: instance name, admin username, password, confirm password. It owns validation and the setup API transaction, then calls an injected completion callback; each platform route wrapper owns navigation. Built from `Form` + `Input` + `Button` + `Alert` inside a `Card`. |
| `LoginForm` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Exported login card built from `Form` + `Input` + `Button` + `Alert`. It delegates credentials to the shared auth context and calls an injected success callback; each platform route wrapper owns return-path navigation. |
| `Tabs` | `packages/ui-kit/src` | shadcn-extended | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-tabs`. `TabsList` uses `.surface-panel` instead of `bg-muted`, so it is identical at the `off` and `standard` blur levels and frosted at `extra` without a second class. Includes `TabsList`, `TabsTrigger`, `TabsContent`. The settings section nav. Driven by the URL rather than internal state, so a settings page is linkable — `SettingsLayout` reads the location and the triggers navigate. `TabsContent` is unused there because the panels are router `Outlet` children. Stock shadcn's `hover:bg-accent` was changed to the neutral `muted` token: in this codebase `accent` is the user's accent colour, not shadcn's neutral hover. |
| `Popover` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-popover`; includes `Popover`, `PopoverTrigger`, and `PopoverContent`. Radix supplies anchored positioning, focus management, Escape/outside dismissal, and portal behavior for the discovery results panel. Content uses `.surface-elevated` and shared border/text tokens so blur and appearance settings apply consistently. |
| `Table` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`, `TableCaption`. Plain semantic `<table>` elements with token styling and no Radix primitive — a table is already the accessible construct for tabular data. `Table` wraps itself in an `overflow-auto` container so a wide table scrolls inside its own box rather than widening the page. Used by `UsersPanel`, `GroupsPanel`, and the grouped/selectable `ConnectorsView`. |
| `Dialog` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-dialog`; the themed replacement for a native `<dialog>`. Radix owns focus trapping, scroll locking, `aria-modal`, and Escape dismissal. Content uses `.surface-elevated` so the blur and reduced-transparency tokens reach it, with contrast from the solid fallback rather than the blur. Holds the create/edit forms in `UsersPanel` and `GroupsPanel`. |
| `Sheet` | `packages/ui-kit/src` | shadcn-extended | `top`, `right`, `bottom`, `left` | web-frontend, desktop, mobile | Wraps `@radix-ui/react-dialog` as an edge-mounted panel with focus trapping, scroll locking, Escape/outside dismissal, and an accessible 44px close target. Content uses `.surface-elevated`. `AppShell` uses the left variant for small-screen navigation. |
| `AlertDialog` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-alert-dialog`. **Deliberately distinct from `Dialog`, not a variant of it:** Radix gives it `role="alertdialog"`, focuses cancel first, and refuses outside-click and Escape dismissal. Reserved for destructive confirmation; anything containing a form is a `Dialog`. `AlertDialogAction`/`AlertDialogCancel` render through `buttonVariants`. Delete flows prevent the default close so backend refusals and bulk per-row results remain visible; `ConnectorsView` also keeps partial bulk failures open for retry. |
| `Tooltip` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-tooltip` with the shared elevated-surface tokens. Its former consumer — the disabled Add connector affordance on the interim dashboard — is gone now that placement editing is real; the pattern it demonstrated (a focusable wrapper so keyboard users can reach the explanation on a disabled control) is the reason to keep reaching for it rather than a `title`. |
| `Switch` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-switch`. Used for active/inactive state in the edit-user dialog, Desktop's explicit invalid-certificate opt-in, and `ActionToggleWidget`. Checked state resolves from `--accent` via Tailwind's `primary`. Motion is a `translate-x` transition that the global `prefers-reduced-motion` rule reduces to an instant swap — state stays visible either way, since thumb position *and* track colour both convey it. |
| `Checkbox` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-checkbox`; the themed replacement for a browser-default checkbox. Radix renders a hidden native input for form participation while the visible box is ours. Used by `GroupMultiSelect`, `PermissionGrantBuilder`, dashboard grouping, and `ConnectorsView` row/select-all controls (including the indeterminate visible-selection state). |
| `Select` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-select`. Used for connector scopes/types, placement bindings, action selectors, and the connector-table sort order. This is the control the no-native-controls rule exists for — a native `<select>` renders differently on every platform. Content uses `.surface-elevated`; item focus uses the `muted` token rather than stock shadcn's `accent`, as with `Tabs`. |
| `UsersPanel` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | `/settings/users`: a `Table` of accounts with status and membership `Badge`s, a create `Dialog`, an edit `Dialog` (`Switch` + `GroupMultiSelect`), and a delete `AlertDialog`. Composition of existing primitives. Handles the backend's 409 safeguards as their own case — the message is shown verbatim because it is written to be read ("that would leave no active administrator" is instruction; "something went wrong" is not). Degrades to raw group ids when `GET /groups` 403s, which is what a `users.manage`-only account sees. |
| `GroupsPanel` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | `/settings/groups`: a `Table` of groups with a Protected `Badge`, one `Dialog` shared by create and edit, and a delete `AlertDialog`. The delete control is **not rendered at all** for a protected group rather than rendered-and-refused — a control existing only to produce a 409 teaches people that errors are normal. One dialog serves both operations because `PATCH` states the whole grant set exactly as `POST` does. |
| `PermissionGrantBuilder` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | The grant editor shared by the create and edit group dialogs — its own component precisely because it has two consumers. Rows come from `GET /permissions`, never a hardcoded list: the `permissions` table is authoritative, so a hardcoded form would be quietly wrong rather than visibly broken after the next migration. Every grant is global except `connectors.control`, which may be narrowed to one connector via `Select`. The scope value is the **instance** id (a UUID) from `GET /connector-instances`, not the connector type id — scoping to a type id would produce a grant the backend silently matches against nothing. That scoping is deliberately specific rather than a generic resource-type picker — `connector` is the only resource type that exists today. Composition of `Checkbox` + `Select` + `Label`. |
| `GroupMultiSelect` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Group membership as a `Checkbox` list rather than a multi-select popover: membership is small and enumerable, and better read all at once than summarised as "3 selected". Controlled, and it states the whole selection on every change, mirroring the API's replace-wholesale `groupIds`. Shown as a disabled explanation when the group list could not be read. |
| `Avatar` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-avatar`; includes `AvatarImage` and `AvatarFallback`. The fallback is the reason to use the primitive rather than a bare `<img>`: Radix tracks the image's load state and swaps in initials when the source is missing, still loading, or broken — which is exactly the set of states an `avatarUrl` has. A hand-rolled version reaches for `onError` and still flashes a broken-image icon first. |
| `SettingsLayout` | `packages/ui-kit/src` | custom | responsive tab strip | web-frontend, desktop, mobile | Top-level General, Account, Appearance, and permission-gated Permissions tabs. The non-shrinking tab strip scrolls horizontally within its own touch-pan region on narrow screens, keeping every section reachable without widening the page. Active state, navigation, shell rendering, and panel content are injected, keeping routing platform-specific while permission visibility remains shared. |
| `PermissionsLayout` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Smaller nested Users/Groups tabs. The sub-nav renders only when the user holds both grants. Active state, navigation, and content are injected; `usePreferredPermissionsSection` shares the permission-based landing decision without importing a router. |
| `GeneralPanel` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Read-only instance information assembled from the shared API client. It remains intentionally sparse until the backend exposes instance configuration. |
| `AccountPanel` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | `/settings/account`: avatar, profile form, and `PasswordChangeForm`. A 409 on the username is surfaced as a **field-level** error rather than a toast — that input is what has to change, and a toast leaves the user staring at a form with no indication of which field is at fault. The avatar section pairs a `sr-only` file input with a `buttonVariants`-styled `<label>`: `sr-only` rather than `display:none` because a hidden input is not focusable, which would put the control out of reach of a keyboard user. Upload errors show the backend's own message (too large, not a decodable image), since the backend decides by decoding the bytes and its wording is the only useful part. |
| `PasswordChangeForm` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Its own component and its own form, deliberately separate from the profile form: it is a separate transaction needing the current password as proof, and combining them would make someone changing their display name think about their password. A 401 means the *current password* was wrong — not an expired session — so it sets a field-level error and **leaves the new-password fields intact**; only success clears the form. The API client opts this call out of its automatic refresh-and-retry so that distinction survives the transport. |
| `AppearancePanel` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | `/settings/appearance`: theme, accent swatches, a custom hex field, a three-level blur `SegmentedControl`, and a motion `Switch`. No save button — every change applies and persists immediately, which is the honest interaction for settings whose whole effect is already visible. The motion switch shows as checked-and-disabled when the OS asks for reduced motion, because it genuinely is on and the app must not be able to lower that floor. Carries an on-screen note that these are per-device. |
| `ColorSwatchPicker` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Accent presets as eight swatches at consistent saturation and lightness, so none reads as conspicuously darker than its neighbours in either theme. Also exports `hslToHex`/`hexToHsl`: the stored form is always bare HSL channels, and hex exists only at the edge where people type colours. Selection is shown by a ring **and** a check mark — a ring around a coloured circle is easy to miss and invisible to someone who cannot distinguish the hues. Still built as `aria-pressed` buttons. `RadioGroup` now exists (added for `SegmentedControl`), so the note that once deferred this no longer applies — converting the swatches to it would gain arrow-key navigation and a single tab stop, and is a small, worthwhile follow-up rather than something this change needed. |
| `RadioGroup` | `packages/ui-kit/src` | shadcn | — | web-frontend, desktop, mobile | Wraps `@radix-ui/react-radio-group`; includes `RadioGroupItem`. The accessible construct for "exactly one of these". Radix supplies the part that is tedious and easy to get wrong: roving focus, so the group is a single tab stop and the arrow keys move between options. |
| `SegmentedControl` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | `RadioGroup` re-skinned as side-by-side labelled segments — the same primitive, a different surface, which is step (b) of the sourcing rule. Used for Theme, Blurred surfaces, the User/Group choice in `DashboardSharesDialog`, the Display/Action choice in `PlacementBindingEditor`, and the Server info/Single container choice in `AddPlacementDialog`. Preferred over `Select` for three-or-fewer short options, where every choice being visible without opening anything is the point. The selected segment reads as a raised surface, matching `TabsTrigger`, so the two controls look related. |
| `AccentThemeProvider` | `packages/ui-kit/src` | custom | — | web-frontend, desktop, mobile | Theme provider, not a visual component. Stateful and `localStorage`-backed: it owns the accent (`--accent`), the blur **level** (`off` / `standard` / `extra`, one root class each), motion (`.reduce-motion`), and the light/dark palette (`.dark` plus `color-scheme`). All are read during `useState` initialisation so no frame paints with the wrong values first. Exposes `useAppearance()` (renamed from `useAccentTheme`, since it is no longer only about accent). The blur setting used to be a boolean; the old `loom-blur-enabled` key is still read once so an existing choice is not silently reset. `prefers-reduced-motion` is a floor the in-app switch may raise but never lower; `prefers-reduced-transparency` and `prefers-color-scheme` are only defaults, which an explicit choice overrides in either direction. All clients consume this provider from the kit. |
