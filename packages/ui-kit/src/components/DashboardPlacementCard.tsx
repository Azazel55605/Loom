/**
 * @deprecated Use `PlacementTile`. Kept as a source-compatible export for
 * consumers compiled against the pre-grouping component name.
 */
export {
  PlacementTile as DashboardPlacementCard,
  DRAG_HANDLE_CLASS,
  type LiveStatus,
} from "@loom/ui-kit/components/PlacementTile";
