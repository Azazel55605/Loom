[package]
name = "loom-core"
description = "Shared library for Loom: connector logic, business logic, and (later) the shared UI kit."
edition.workspace = true
license.workspace = true
version = "0.2.3"
repository.workspace = true
rust-version.workspace = true

[dependencies]
async-trait.workspace = true
chrono.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
# Only for `tokio::time::sleep`, which `DebugConnector` needs to simulate
# latency; core spawns nothing and owns no runtime.
tokio.workspace = true
