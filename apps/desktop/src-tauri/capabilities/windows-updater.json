{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-updater",
  "description": "Main-window access to the native updater on Windows only. See ADR 0037.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": ["updater:default"]
}
