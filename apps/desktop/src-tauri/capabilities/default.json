{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window access to non-sensitive server settings and OS-backed credential storage. Network hosts are runtime-selected; see ADR 0010.",
  "windows": ["main"],
  "permissions": ["core:default", "store:default", "keyring-store:default"]
}
